using System;
//using Quant.DB.CLI.Models;

namespace Quant.DB.CLI
{
    class Program
    {
        static void Main(string[] args)
        {
            //    using (var ctx = new QuantContext())
            //    {
            //        var res = ctx.Add<Payload>(
            //            new Payload
            //            {
            //                Content = ctx.ToString()
            //            }).Entity;

            //ctx.SaveChanges();

            //        Console.WriteLine(res.PayloadId);
            //        Console.ReadKey();
            //    
        }
    }
}
